#include "Main.h"
#include "Mob.h"
#include <iostream>
#pragma once

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie();
	std::cout.tie();
	srand(time(NULL));
	init();
	std::cout << "1\n";
	return 0;
}
